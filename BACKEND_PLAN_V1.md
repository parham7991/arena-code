# پلن بک‌اند Arena Code — پله‌پله، هر مرحله بی‌نقص

**نسخه:** v1.0 — تاریخ: 2026-08-10 — وضعیت: منتظر دستور مرحله 1
**اصل:** هیچ عدد حدسی نیست — همه از سورس دقیق (`limits.mjs`).

---

## اصول هر مرحله (Definition of Flawless)

هر مرحله فقط وقتی تمومه که:
1. ✅ تست‌ها پاس (`npm test` — همون 58 تست فعلی + تست‌های جدید مرحله)
2. ✅ لیمیت دقیق رعایت شده (20k پیام، 50k Bash، 8 Tool، 5MB Body)
3. ✅ کامیت جدا + پوش به `main` + تگ `backend-stage-N`
4. ✅ تو تایید کنی — بدون تایید تو مرحله بعد شروع نمیشه

---

## نقشه 5 مرحله‌ای بک‌اند

### مرحله 1: فونداسیون بی‌نقص (Foundation) — 1 روز
**هدف:** هیچ پیامی truncated/compact نشه

| کار | دقیق چیه | فایل |
| :--- | :--- | :--- |
| 1.1 | `limits.mjs` منبع واحد شد (الان هست) — تست `compactInfo` | `src/limits.mjs` |
| 1.2 | `chunker.mjs` هوشمند 20k (الان هست) — تست 50000 -> 3 تیکه | `src/chunker.mjs` |
| 1.3 | اتصال به `format.mjs` + `bridge.mjs` — قبل `send` چک `isOverLimit` و auto-chunk | `src/agent.mjs` |

**DoD مرحله 1:** فایل 100KB بفرستی → 5 تیکه 20k با `[[PART]]` میره، Arena کامل میبینه، هیچ `...[compacted]` نمیاد.

---

### مرحله 2: ابزارهای هسته 10→15 (Core Tools) — 2 روز
**هدف:** 5 برابر Claude Code در اجرا

| ابزار جدید | چرا |
| :--- | :--- |
| `Process` (الان هست) | `npm run dev` روشن نگه دار |
| `Test` (الان هست) | `npm test` خودکار |
| `Diagnostics` (الان هست) | `tsc + eslint` قبل تحویل |
| **+ LSP_Diagnostics** | `tsc --noEmit` لایو — هر Write ارور بده نذاره |
| **+ Git** | `status/diff/log/commit` — بدون خروج از Agent |

**DoD:** `registry` = 15 ابزار، هر کدوم تست جدا، `getToolSchemas()` شامل همه.

---

### مرحله 3: حلقه خود-ترمیم (Self-Healing Loop) — 3 روز
**هدف:** خودش کد بزنه، خودش تست بگیره، خودش فیکس کنه

```
Write -> Diagnostics (اگر فیل -> فیکس) -> Test (اگر فیل -> فیکس) -> تحویل
```
پیاده در `src/agent.mjs` → هوک `onToolAfter`

**DoD:** فایل با ارور تایپ بسازه → Agent خودکار فیکس کنه تا `Diagnostics.passed=true`، بعد تحویل.

---

### مرحله 4: ران و پروسس پایدار — 2 روز
**هدف:** خودش ران کنه، باگ نخوره

*   `Process` + `HTTP_Fetch` + `Port_Check`
*   `Bash` با 50k cap + auto `tail -n 200` اگر truncated

**DoD:** `npm run dev` -> `Process start` -> `HTTP 200` -> `logs` بدون truncated.

---

### مرحله 5: امنیت و ریت‌لیمیت دقیق — 1 روز
**هدف:** هیچوقت 429/503 نخوره

*   `RateLimiter` دقیق 100 rpm + `Queue 8` با backoff نمایی (کد `server.mjs` همینه)
*   `credentials` با 0600 + AES

**DoD:** 20 درخواست موازی -> 8 تا صف، 12 تا با Retry-After درست برمیگرده.

---

## ترتیب پیشنهادی (تو انتخاب کن)

**پیشنهاد من:** 1 → 2 → 3 → 4 → 5

ولی تو بگو از کدوم شروع کنیم — من فقط همون یکی رو میسازم، تست میگیرم، کامیت میزنم، منتظر تایید تو میمونم.

**بگو: "مرحله 1 رو شروع کن" یا "مرحله 2" یا هر مرحله‌ای که میخوای.**
